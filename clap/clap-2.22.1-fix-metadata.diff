--- clap-2.22.1/Cargo.toml	2017-03-24T14:06:50+01:00
+++ clap-2.22.1/Cargo.toml	2017-03-30T10:37:24.599458+02:00
@@ -24,7 +24,6 @@
 ansi_term = { version = "0.9.0",  optional = true }
 term_size = { version = "0.2.3",  optional = true }
 yaml-rust = { version = "0.3.5",  optional = true }
-clippy    = { version = "~0.0.118", optional = true }
 atty      = { version = "0.2.2",  optional = true }
 
 [dev-dependencies]
@@ -38,8 +37,6 @@
 wrap_help   = ["term_size"]
 yaml        = ["yaml-rust"]
 unstable    = [] # for building with unstable clap features (doesn't require nightly Rust) (currently none)
-nightly     = [] # for building with unstable Rust features (currently none)
-lints       = ["clippy"] # Requires nightly Rust
 debug       = [] # Enables debug messages
 no_cargo    = [] # Enable if you're not using Cargo, disables Cargo-env-var-dependent macros
 
